# Frontend

React + [Vite](https://vitejs.dev/) + MUI.

```sh
npm install
npm run dev      # dev server on :3000
npm run build    # production build -> ../build
npm test         # vitest
```

See the root [README.md](../README.md) for full run instructions.
