# Required once you use anything auth-related — login will fail loudly with a clear error
# if this is unset, rather than falling back to an insecure default. Generate one with:
#   python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=

# Optional. If unset, backend/ defaults to a local SQLite file at backend/database.sqlite.
DATABASE_URL=

# Optional, JSON array of allowed origins. Defaults to ["http://localhost:3000"] (the Vite
# dev server).
# CORS_ORIGINS=["https://your-domain.example"]
